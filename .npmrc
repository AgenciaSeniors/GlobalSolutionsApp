# El plugin @codetrix-studio/capacitor-google-auth declara peer @capacitor/core@^6,
# pero el proyecto usa Capacitor 8. Sin esto, `npm install`/`npm ci` falla con ERESOLVE
# (local, VPS y CI). legacy-peer-deps permite resolver el árbol igual que antes.
legacy-peer-deps=true
